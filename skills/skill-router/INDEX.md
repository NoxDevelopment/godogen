# Skill Registry Index

_39 skills. Auto-generated by `skill-router/tools/router_index.py` — do not hand-edit._

| Skill | Domains | Purpose |
|---|---|---|
| `3d-asset-pipeline` | art/2d · art/3d · engine · pipeline | PNG → GLB via Tripo3D, with quality presets, batch processing, and one-shot prop generation that chains image-pipeline (txt2img) → mesh → engine sidecars in a single command. |
| `accessibility` | ui/ux · gameplay · meta/process | A discipline skill (no tools — rules that constrain how the game is built). Games default to *inaccessible*: tiny text, color-only signals, mandatory fast inputs, flashing effects, no captions.… |
| `animation-pipeline` | art/2d · animation · pipeline | Sprite animation cycles, frame interpolation, and sprite-sheet assembly. Generates temporally-phased animations (idle / walk / run / attack / hurt / death / jump / cast) by running one… |
| `asset-manifest` | art/2d · art/3d · animation · pipeline | A single, durable index of every generated asset in the project — its origin (image-pipeline / 3d-asset-pipeline / scene-art / animation-pipeline / etc.), its SHA, the prompt + style + preset that… |
| `asset-reuse` | narrative · pipeline | Fresh generation is the most expensive, least consistent way to get an asset. Every asset request goes through this ladder; generation is the bottom rung. |
| `audio-pipeline` | audio · pipeline | All-in-one audio generation: procedural sound effects, scale-aware music, and TTS speech via local providers (Kokoro → Orpheus → EdgeTTS fallback chain). All free, all local. |
| `blender-bridge` | art/3d · narrative · pipeline | Install of record: Blender 4.3 at D:\Blender Foundation\Blender 4.3\. Purchased addons/assets: NAS \\DXP4800PLUS-A79\NoxDev\ (blender-tools-and-assets\, superhive-creature-character-bundle\). |
| `camera-rigs` | art/3d · ui/ux · engine | Godot 4 camera rig scaffolds: Camera2D for platformers and top-downs, Camera3D for third-person and first-person, plus screen-shake mixin and bounds clamp. Each rig is a self-contained .tscn + .gd… |
| `character-mime` | animation · audio · narrative | Fidelity ladder: volume/VU < MFCC vowel-class < TTS phoneme timestamps → viseme table < neural audio→blendshape (adds co-articulation + emotion). Pick the lowest rung that reads well —… |
| `character-sheet` | art/2d · ui/ux · narrative | Generate a 3×3 grid of poses for one character in a single ComfyUI call, then post-process — magenta-key the background, slice into 9 cells, tight-crop each sprite, pad to a target aspect, and… |
| `companion-npcs` | narrative | Bake deep NPCs from companion_ai_core — the 240k-line pure-Dart companion generator (12,000+ attributes per companion: personality, culture, memories, emotions, relationships, physical… |
| `council` | ui/ux · engine · meta/process | A decision skill. Run a hard, open-ended call — a roadmap ordering, an engine choice, a scope cut, a naming decision, a "should we even build this" — through a council of five independent advisors… |
| `daz-bridge` | art/3d | Per noxdev-daz-licensing/ADR-001 (2026-06-27): |
| `design-review` | ui/ux · engine · meta/process | A UI/UX quality gate for everything with a surface — the Studio web app (Next.js/React) *and* in-game UI (Godot Control nodes). It turns "looks fine to me" into a repeatable review against our own… |
| `engine-export` | art/2d · art/3d · audio · ui/ux · engine · pipeline | Take a finished asset (sprite, spritesheet, tileset, audio, video, mesh) and emit engine-native scaffolds so it lands ready-to-use in Godot 4 or Unity. Pure text — no ComfyUI / Tripo3D / model… |
| `game-feel` | ui/ux · gameplay · engine · meta/process | The "juice" layer. Good gameplay loops still feel flat without feedback on impact — this skill emits a drop-in GameFeel autoload that gives a Godot 4 game hit-stop, slow-mo punches, damage… |
| `godogen` | engine | Generate and update Godot games from natural language. |
| `godot-task` | engine · meta/process | All files below are in ${CLAUDE_SKILL_DIR}/. Load progressively — read each file when its phase begins, not upfront. |
| `if-engine` | narrative · engine | The deterministic foundation the whole gamebook expansion layers onto: computed-core first, AI as a layer. Every mechanic runs without an LLM and without networking — a classic text-RPG driven by… |
| `image-pipeline` | art/2d · art/3d · pipeline | Generate game art that matches the visual target, with the right tool for the right asset type. Use this whenever you need to produce an image — prefer this over any other image-generation path. |
| `input-handling` | art/3d · ui/ux · gameplay · engine | Godot 4 InputMap action emission, runtime rebinding-UI scaffolds, and conflict / dead-zone diagnostics. Pure text — no ComfyUI / Tripo3D / model dependencies. |
| `narrative` | audio · narrative · engine | Dialogue tree converters (Ink / Yarn / Dialogic), quest schema validation + emission (Godot .tres / Unity ScriptableObject JSON), markdown world-bible scaffolder, and NPC voice batch (chains… |
| `netcode` | gameplay · engine | Make a scaffolded Godot 4 genre template multiplayer. One drop-in, two profiles, a host-authoritative model, and a transport chosen for the profile. Scenes never change — the layer intercepts at… |
| `pixel-perfect` | art/2d | AI image models cannot produce grid-true pixel art: pixels drift in size and position, the grid wobbles across the image, and colors smear off-palette. Every "pixel art" output is FAKE pixel art… |
| `playtest` | ui/ux · gameplay · engine | Godot 4 headless runner + checkpoint autoload + markdown report generator. Drives godot --headless against a project, captures periodic screenshots and stdout/stderr, then collates everything into… |
| `provider-preflight` | pipeline | Run before any long-running asset batch. Verifies every external provider the godogen pipeline depends on is reachable, configured, and within budget — so a 50-asset batch doesn't half-succeed… |
| `save-system` | art/3d · ui/ux · gameplay · engine | Godot 4 save-system scaffold: typed Resource save model, multi-slot manager with thumbnails, version migration chain, autosave timer, and atomic write-with-fsync. Pure text — no ComfyUI / Tripo3D… |
| `scene-art` | art/2d · engine | Generators for scene-level game art that goes beyond single sprites: parallax backgrounds, skyboxes, tilesets, and wide-aspect environment references. Outputs are written in Godot- and… |
| `scene-populate` | art/3d · ui/ux · engine | Turn "build me a forest clearing" into a dressed, runnable .tscn: trees ring a clearing, ferns and grass carpet the ground (batched into MultiMesh), rock clumps break up the floor, a lantern-lined… |
| `shader-craft` | art/3d · animation · ui/ux · engine | Godot 4 .gdshader and Unity ShaderLab/HLSL generators for five common game shaders with sensible defaults. Pure text emission — no ComfyUI / Tripo3D / model dependencies. Each shader ships with… |
| `skeleton-rig` | art/2d · art/3d · ui/ux · narrative · pipeline | Render stick-figure pose images (OpenPose-style skeletons) from a built-in pose library or custom joint coordinates. Pair with image-pipeline's --reference flag (img2img) to bend a character… |
| `skill-router` | pipeline · meta/process | A preflight for the skill registry. We have ~40 skills and the number keeps growing. Reading all of them to decide which one fits a task is wasteful, and *not* reading them means reinventing what… |
| `style-anchor` | art/2d · pipeline · meta/process | reference.png is ground truth. Every asset must match its palette, perspective, scale, and rendering style. This skill is the discipline layer on top of the… |
| `ui-elements` | art/2d · ui/ux · pipeline | Generate individual UI sprite assets — buttons (with hover/pressed states), icons, healthbars (frame + fill), panels (9-slice-ready) — at sprite resolutions appropriate for game HUDs. Wraps… |
| `ui-screens` | ui/ux · engine | Godot 4 Control-tree .tscn and Unity Canvas-layout JSON scaffolds for the five common game screens: title, menu, hud, inventory, dialog. Each subcommand emits a sensibly-laid-out scene that drops… |
| `ui-theme` | art/2d · ui/ux · engine · meta/process | Generate a project-styled Godot 4 theme.tres from the project palette, so the [ui-screens](../ui-screens/SKILL.md) scaffolds stop rendering in Godot's default gray theme and instead look like… |
| `vfx-particles` | animation · engine | Emit Godot 4 GPUParticles2D .tscn presets — the particle layer that sells impact: the burst on a hit, dust under a jump, sparkle on a pickup, confetti on a win. Distinct from shader-craft… |
| `video-pipeline` | art/3d · pipeline | LTX 2.3 video generation tuned for 8 GB VRAM rigs via Deno's workflow architecture (GGUF Q4 UNet + Gemma 3 12B FP4 text encoder + RIFE frame interpolation + RTX VSR upscale, with explicit VRAM… |
| `world-layout` | gameplay | Author maps deliberately. Random tile placement produces "streets everywhere" — the most common failure mode in agent-generated games. Before any tile is placed, sketch the layout as ASCII,… |
